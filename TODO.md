# New API Status Bot TODO

本清单是实现顺序和完成判定的唯一入口。插件以 New API 管理日志为主数据源，
只在单模型查询时使用公开 `perf-metrics` 接口补充参考数据。

## P0 - 仓库与契约

- [x] 建立独立 `cdylib` Rust 2024 工程，MSRV 1.89。
- [x] 固定动态插件 ID、命令名、配置结构和数据库路径。
- [x] 提供不含凭据的 `config.example.toml`。
- [x] 提供构建、安装、配置、热重载和故障排查文档。
- [x] 确认所有构建产物、SQLite 文件和本地配置均被忽略。

完成条件：`cargo metadata` 成功，文档可以指导从零构建和安装。

## P1 - 配置与生命周期

- [x] 解析宿主传入的 `PluginInitConfig.config_json`。
- [x] 从配置指定的环境变量读取管理访问令牌，不允许令牌落入 TOML。
- [x] 校验 API URL、管理员 UID、模型白名单、轮询周期和保留天数。
- [x] 创建全局运行状态，避免重复初始化。
- [x] `#[init]` 启动唯一采集线程。
- [x] `#[shutdown]` 使用条件变量唤醒线程并等待 `join`。
- [x] 远端暂时不可用时插件降级启动；本地配置或数据库不可用时拒绝启动。

完成条件：反复 init/shutdown 不泄漏线程，不发生重复采集。

## P2 - New API 客户端与日志采集

- [x] 实现 `GET /api/log/` 管理日志客户端。
- [x] 请求头使用 `Authorization: Bearer <token>` 和 `New-Api-User`。
- [x] 支持 `p`、`page_size`、`start_timestamp`、`end_timestamp`、`model_name`。
- [x] 单页限制 100，完整处理分页响应和业务层 `success=false`。
- [x] 默认按白名单模型拉取，保留全局拉取扩展点。
- [x] 使用时间游标减重叠窗口，避免延迟日志遗漏。
- [x] 首次启动执行可配置回溯，默认 24 小时。
- [x] 单轮页数和请求时间有上限，积压通过自适应时间分段在后续轮次继续追赶。
- [ ] 对鉴权、限流、超时、5xx 分别分类，并指数退避。
- [ ] 解析 `other` 中的 `frt`、错误码、状态码、流结束状态和重试渠道链。

完成条件：模拟分页、乱序、重复、延迟到达和失败响应均能稳定采集。

## P3 - SQLite 与请求归并

- [x] 建立版本化数据库迁移基线（`user_version=1`）。
- [x] 建立 `log_samples` 表保存脱敏日志样本。
- [x] 建立 `request_outcomes` 表保存请求级最终结果。
- [x] 建立 `collector_state` 表保存每模型游标和采集健康状态。
- [x] 建立 `perf_metric_cache` 表，为后续持久化广场缓存保留结构。
- [x] 使用稳定 `sample_key` 去重，错误样本包含推导的重试序号。
- [x] 按 `request_id` 将多条错误尝试和最终消费日志归并。
- [x] 结果状态支持 `pending/success/failed/partial_failed`。
- [x] 流式 `stream_status=error` 默认判定为部分失败。
- [x] 错误后等待结算宽限期再判定最终失败。
- [x] 每日删除超过 7 天的数据并执行 WAL checkpoint。
- [x] 不保存 IP、令牌、完整请求内容；错误摘要必须脱敏和截断。

完成条件：同一批日志重复导入不改变统计，重启后游标可继续。

## P4 - 指标与状态判定

- [x] 预计算 15 分钟、1 小时、24 小时、7 天窗口。
- [x] 按模型和分组统计请求数、成功数、失败数、部分失败数。
- [x] 计算请求正常率、请求错误率、尝试错误率和重试率。
- [x] 计算 TTFT 平均值、P50、P95。
- [x] 计算总体耗时平均值、P50、P95。
- [x] 拒绝负数、明显超过总耗时的 TTFT 异常值。
- [x] `use_time` 按秒级精度展示，不伪造成精确毫秒。
- [x] 支持全局阈值和单模型阈值覆盖。
- [ ] 状态支持 `normal/degraded/abnormal/no_data/stale/insufficient_samples`。
- [x] 模型总体状态取所有展示分组中的最差状态。
- [x] 自动跨组错误无法确认分组时归入“自动路由/未确认”。

完成条件：所有比例和百分位均有确定公式和边界测试。

## P5 - QQ 命令与广场补充

- [x] `/模型状态 [窗口] [模型]` 展示白名单状态。
- [x] `/模型列表` 展示全部白名单、别名、配置分组和数据新鲜度。
- [x] `/模型异常 [模型] [窗口]` 展示脱敏错误分类。
- [x] `/监控健康` 仅管理员可用，展示采集器和数据库状态。
- [x] `/监控刷新` 仅管理员可用，只唤醒后台线程，不在回调里抓日志。
- [x] 命令仅允许配置的 QQ 群，管理命令另受 QQ 用户白名单限制。
- [x] 长消息按模型边界拆分，单条消息长度可配置。
- [x] 单模型查询按需调用 `/api/perf-metrics`，总超时不超过 3 秒。
- [x] 广场失败不影响本地日志状态返回。
- [x] 广场 `avg_ttft_ms=0` 显示为无有效样本。
- [x] 广场数据独立标注，不与日志统计混算。

完成条件：所有命令在无数据、过期、部分失败和超长白名单下均可读。

## P6 - 定时推送与告警

- [x] 注册 `meta/Heartbeat` 动态路由。
- [x] 支持 `periodic/change/anomaly` 三种推送模式。
- [x] 支持指定发送机器人 `self_id`，避免多机器人重复推送。
- [x] 支持多个目标群。
- [x] 状态连续命中两次才告警，恢复也需连续确认。
- [x] 实现告警冷却和相同报告指纹去重。
- [x] 心跳缺失时不阻塞采集，并在健康命令中显示推送不可用。

完成条件：多次心跳不会重复发送，同一异常不会刷屏，恢复通知只发一次。

## P7 - 验证、发布与运维

- [x] 单元测试覆盖 DTO、`other` 解析、脱敏、去重和窗口解析。
- [ ] 集成测试覆盖 HTTP 分页、退避、SQLite 迁移、保留期和重启恢复。
- [ ] 场景测试覆盖“失败后重试成功”“全部失败”“流式部分失败”。
- [ ] 测试热重载时后台线程在超时内退出。
- [x] 执行 `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`。
- [x] 执行 `cargo test` 和 `cargo build --release`。
- [x] 将二进制复制到 QimenBot `plugins/bin/` 做本地加载测试，不改框架源码。
- [ ] 使用新管理访问令牌执行一次最小只读联调。
- [x] 检查 Git 中不存在令牌、数据库和构建产物。
- [x] 更新 README、开发记忆和本清单完成状态。

完成条件：本地验证全部通过，QimenBot 可加载、查询、推送和热重载插件。

## 上线输入

- [ ] 有效的 New API 管理访问令牌已写入宿主 `.env`。
- [x] 管理员 UID 已确认。
- [ ] 模型白名单、别名和可选分组过滤已确认。
- [ ] 允许查询的 QQ 群和管理 QQ 用户已确认。
- [ ] 定时推送目标群、周期和模式已确认。

## 当前未完成项说明

- 管理 API 仍需更换后的有效访问令牌；此前暴露且验证失败的令牌不会写入配置或重试。
- `perf_metric_cache` 目前只有数据库结构，命令路径使用进程内 TTL 缓存；持久化不是主日志统计的上线前置条件。
- 需要补齐 HTTP 多页/退避、保留期、重启恢复和真实热重载集成测试。
- 需要将鉴权、限流、超时和 5xx 细分为稳定的采集健康错误码，并验证退避行为。
- 模型状态尚未增加独立 `stale` 枚举；当前由报告头和健康命令明确标记数据过期。
- 完整模型白名单、QQ 群、管理员 QQ、推送目标及模式仍需上线参数。
